{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build", // <- argument is called in the launch.json prelaun. parameter
      "type": "shell",
      "command": "g++",
      "args": ["-std=c++20", "-g", "*.cpp", "-o", "output"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
]

}
